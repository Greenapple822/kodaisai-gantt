# 工大祭 設営ガントチャート生成ツール

## このプロジェクトの目的

放送研究会（映像班・音響班・照明班）の工大祭ステージ企画における、設営・本番・撤収の作業計画を可視化するツール。
部員全員が当日参照する。実行委員会への提出資料にもなる。

## 絶対に守る設計方針

### 1. 単一データソース
`data/schedule.csv` 1枚から、すべてのビューを生成する。
図ごとにデータを持たせない。作業が1つ増えたときに直す場所は1箇所だけにする。

### 2. ビルド不要の単一 HTML を最終成果物にする
- フレームワーク（React / Vue / Svelte）を使わない
- npm 依存を持たない
- 最終成果物は `dist/index.html` 1ファイル。ダブルクリックで開ける
- 理由：来年以降の部長に引き継ぐ。Node.js も Claude Code も無い環境で編集・実行できる必要がある

### 3. file:// 対策
`fetch()` はローカルファイルで CORS に阻まれる。
`tools/build.py`（Python標準ライブラリのみ）で CSV と JSON を HTML に文字列として埋め込み、`dist/index.html` を出力する。
開発中は `python -m http.server` を使ってよいが、配布物は必ず埋め込み版。

### 4. 会場レイアウトをハードコードしない
エリアの座標は `data/layout.json` に持つ。会場が変わったら JSON だけ差し替えれば動くこと。

会場そのものは画面からも作り替えられる（編集タブ「場所（会場の配置）」。
ドラッグで移動、つまみで大きさ、追加・削除・改名）。変更はレイアウトまるごとの上書きとして
案と共有保存に載せ、`layout.json` そのものは書き換えない。既定と同じ形なら上書きを持たない。
**`schedule.csv` の `area` は必ず id を指す。** 名前を変えても動かしても割り当てが壊れないのはこのため。
使われている場所を消しても CSV は書き換えず、警告で知らせるだけにする（戻せば直る）。
旧形式（名前だけの差分 `{"foh": "PA卓"}`）も読めるようにしてある。

## ファイル構成

```
kodaisai-gantt/
├── CLAUDE.md
├── README.md              部員向けの使い方（非エンジニア前提で書く）
├── data/
│   ├── schedule.csv       作業データ（唯一の真実）
│   ├── layout.json        会場エリアの座標
│   └── share.json         みんなの予定の置き場所（無ければ共有保存を出さない）
├── src/
│   ├── index.html
│   ├── app.js
│   └── style.css
├── tools/
│   ├── build.py           CSV/JSON を HTML に埋め込んで dist/ docs/ を出力
│   └── publish.sh         ビルド＋commit＋push を1コマンドにしたもの
├── docs/
│   └── index.html         GitHub Pages が配信する。中身は dist/index.html と同じ
└── dist/
    ├── index.html         配布用。これだけ配れば動く
    └── artifact.html      claude.ai で共有するとき用の断片
```

## 公開

GitHub Pages で配信する。部員はログイン不要でブラウザから見られる。

- 公開URL：<https://greenapple822.github.io/kodaisai-gantt/>
- リポジトリ：<https://github.com/Greenapple822/kodaisai-gantt>（public / main ブランチの `/docs`）
- 更新は `sh tools/publish.sh` だけ。CSV を直したあとこれを叩けば1〜2分で全員の画面が変わる

### 保存の3層

1. **配布時の予定**：ページに焼き込んだ CSV。ネットが死んでも必ず出る。唯一の真実
2. **みんなの予定**：`data/share.json` が指す置き場所（textdb.dev）に CSV をそのまま預ける。
   ログイン不要で全員が読み書きでき、20秒ごとに取り込む。自分の編集を消さないよう、
   衝突したら本人に選ばせる
3. **自分だけの案**：localStorage。端末ごとに独立

「やらないこと」に「サーバー・DB」と書いたが、**2 は部長の依頼で例外として入れた**。
理由：ログイン不要のまま全員で同じ予定を見たい、という要求が
静的ホスティングだけでは満たせなかったため。制約は次のとおりで、これを承知で使っている。

- URL を知っている人は誰でも書き換えられる（認証がない代わり）
- 無料の外部サービスなので、消えても壊れてもおかしくない
- したがって **確定版は必ず `data/schedule.csv` に戻して publish する**。
  みんなの予定はあくまで作業中の黒板とする
- 置き場所が落ちていてもページは動くこと（焼き込んだ CSV で表示を続ける）を必ず保つ

## データ形式

### data/schedule.csv

| 列 | 内容 | 例 |
|---|---|---|
| id | 一意のID | A2 |
| team | 映像 / 音響 / 照明 / 全班 | 音響 |
| area | layout.json の area id と一致させる | stage |
| task | 作業名（12文字以内） | スピーカー・卓設置 |
| start | HH:MM | 10:00 |
| end | HH:MM | 12:00 |
| owner | 担当者名（複数可、`・`区切り） | 山田・佐藤 |
| depends | 依存する作業のid（複数可、`,`区切り）。空欄可 | P1 |
| note | 備考。空欄可 | 脚立2台使用 |
| kind | 行の種類。空欄 / `milestone` / `resource` | resource |

- `team` が複数班にまたがる作業は行を分けず `全班` とする
- `depends` は遅延伝播の計算に使う
- `kind` は単一データソースを守るために追加した列。ビュー1が要求する
  マイルストーンと共有リソースを、別ファイルを作らずに CSV 1枚で表すためのもの
  - 空欄：ふつうの作業。班レーンに描く
  - `milestone`：全体の節目。`start` と `end` を同じ時刻にする。全レーン貫通の縦破線になる
  - `resource`：排他的に使う資源（ステージ占有・脚立・電源）。共有リソースレーンに描く

### data/layout.json

```json
{
  "areas": [
    { "id": "stage",  "name": "ステージ上", "x": 145, "y": 50,  "w": 390, "h": 120 },
    { "id": "wingL",  "name": "下手袖",     "x": 40,  "y": 50,  "w": 90,  "h": 120 }
  ],
  "viewBox": "0 0 680 400"
}
```

## 生成するビュー

すべて同じ CSV から生成。時間軸の目盛りは3ビューで完全に一致させること。

### ビュー1: 班別ガントチャート
- 縦軸 = 班（スイムレーン）、横軸 = 時間
- 一番下に「共有リソース」レーンを置く。ステージ占有・電源・脚立など、排他的に使う資源の状態を示す
- バーに担当者名を併記する
- マイルストーン（電源投入 / 音出し可能 / 通しリハ開始 / 開場）を全レーン貫通の縦破線で

### ビュー2: 場所別ガントチャート
- 縦軸 = 会場エリア、横軸 = 時間
- バーの色 = 担当班
- 同じエリア・同じ時間に複数班がいる場合、行内で上下に分割して両方表示する。重ねて隠さない
- **重複検出**：同一エリアで3班以上が重なる時間帯、または `共有リソース` が競合する時間帯は警告色でハイライトする。これがこのツールの一番の存在意義

### ビュー3: 平面図タイムライン
- `layout.json` の座標で会場平面図を描画
- 時刻スライダー（15分刻み）で、その時刻に各エリアにいる班を色分け表示
- 作業がないエリアは淡色

## 当日運用のための機能

優先度順。上から実装する。

1. **現在時刻の縦線**：全ガントビューに、現在時刻を示す縦線をリアルタイム表示。設営中に袖のタブレットで表示しておく用
2. **印刷レイアウト**：`@media print` で A4横。班別ガント1枚、平面図を時刻ごとに6コマ割りしたもの1枚。当日は電源もネットも保証されないため紙が必須
3. **遅延入力**：任意の作業に「+15分」「+30分」を適用すると、`depends` を辿って後続作業が連動してずれる。元データは書き換えず、画面上のシミュレーションとして扱う
4. **班フィルタ**：特定の班だけ表示。部員が自分の班だけ見たいとき用

## コーディング規約

- バニラ JS。ES2020 まで。トランスパイルしない
- 図は SVG を JS で組み立てる。Canvas は使わない（印刷とテキスト選択のため）
- ダークモード対応は不要。印刷前提のため常にライトテーマ
- 日本語テキストは SVG で自動折り返しされない。作業名が枠に収まらない場合は省略記号を付ける処理を入れる
- 色は班に固定で紐づける。定数として1箇所にまとめる

## やらないこと

- ユーザー認証、サーバー、DB
- 香盤表（本番進行台本）の管理。これは別物なので混ぜない
- 機材管理・在庫管理
- リアルタイム共同編集

## 最初のタスク

1. `data/schedule.csv` と `data/layout.json` にサンプルデータを作る（設営当日 8:00〜13:00、3班分、10〜15タスク）
2. `src/index.html` + `app.js` で CSV をパースし、ビュー1（班別ガント）だけを描画する
3. `tools/build.py` を書いて `dist/index.html` が単体で開けることを確認する
4. ここまで動いてからビュー2、ビュー3に進む
