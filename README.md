# 工大祭 設営ガントチャート

設営・本番・撤収の作業計画を1枚の図にするツールです。
**当日見るだけの人は `dist/index.html` をダブルクリックしてください。** それだけで開きます。

---

## 1. 予定を変更したいとき

直す場所は **`data/schedule.csv` の1箇所だけ** です。
Excel でも Google スプレッドシートでも、メモ帳でも開けます。

**CSVを直接さわりたくない人は、画面の「編集」タブから直せます**（→ 6-3）。
編集した結果はCSVとして書き出せるので、最後に反映する場所はどちらの方法でも `schedule.csv` 1枚のままです。

| 列 | 何を書くか | 例 |
|---|---|---|
| `id` | かぶらない名前。班の頭文字＋番号で十分 | `S3` |
| `team` | `映像` / `音響` / `照明` / `全班` のどれか | `音響` |
| `area` | 場所。`data/layout.json` にある id を書く | `stage` |
| `task` | 作業名。**12文字以内**。長いと図で「…」になる | `配線・結線` |
| `start` | 開始時刻 `HH:MM` | `10:00` |
| `end` | 終了時刻 `HH:MM` | `11:00` |
| `owner` | 担当者。複数なら `・` で区切る | `山田・伊藤` |
| `depends` | 先に終わってないと始められない作業の `id`。複数なら `,` で区切る | `S1,S2` |
| `note` | 備考。空でよい | `脚立2台使用` |
| `kind` | 空 = ふつうの作業 / `milestone` / `resource`（下記） | |

### `kind` の使い分け

- **空欄**：ふつうの作業。班のレーンにバーで出る
- **`milestone`**：全体の節目（電源投入・音出し可能・通しリハ開始・開場）。
  `start` と `end` を同じ時刻にする。図では全レーンを貫く赤い縦破線になる
- **`resource`**：取り合いになる資源（ステージ占有・脚立・電源など）。
  図の一番下の「共有リソース」レーンに出る。誰かと誰かがぶつかってないか確認するための行

### 使える `area`

`data/layout.json` に書いてある id だけです。いまは
`stage`（ステージ上）/ `wingL`（下手袖）/ `wingR`（上手袖）/ `backstage`（楽屋）/
`floor`（客席）/ `foh`（客席後方・卓）/ `storage`（機材置場）。

会場が変わった年は `data/layout.json` の座標だけ差し替えれば図が追従します。

---

## 2. 変更を反映する（配布用を作り直す）

CSV を直したら、ターミナルでプロジェクトのフォルダに入って1行打つだけです。

```bash
python3 tools/build.py
```

`dist/index.html` が作り直されます。あとはこの1ファイルを部員に配ってください。
（Python は macOS に最初から入っています。npm も Node.js も要りません）

> **なぜビルドが要るの？**
> HTML をダブルクリックで開くと、ブラウザの制限で CSV を読みに行けません。
> そこで CSV と JSON を HTML の中に文字列として焼き込んでいます。

---

## 3. データが間違っているとき

図の上にオレンジ色の枠で「データの確認が必要です」と出ます。
存在しない `area`、見つからない `depends`、`end` が `start` より前、などをその場で教えてくれます。
出たら CSV を直して、もう一度 `python3 tools/build.py` してください。

---

## 4. 開発するとき

`src/` を直接ダブルクリックしても動きません（同じブラウザの制限のため）。

```bash
python3 -m http.server 8000
```

を叩いて `http://localhost:8000/src/` を開いてください。
この状態なら CSV を保存 → ブラウザを再読み込みするだけで反映されます。
配る前には必ず `python3 tools/build.py` を実行してください。

---

## 5. ファイルの置き場所

```
kodaisai-gantt/
├── CLAUDE.md              設計方針（いじる前に読む）
├── README.md              これ
├── data/
│   ├── schedule.csv       設営日の作業データ
│   ├── shifts.csv         本番当日のシフト
│   └── layout.json        会場エリアの座標
├── src/                   開発用（index.html / app.js / style.css）
├── tools/build.py         配布用を作るスクリプト
└── dist/
    ├── index.html         配布用。これだけ配れば動く
    └── artifact.html      リンク共有ページ用。中身は index.html と同じ
```

## 6. 画面の使い方

上のタブで2つのビューを切り替えます。

### ビュー1　班別ガント

班ごとの横棒グラフです。一番下の「共有リソース」は、ステージ占有・脚立・電源など
取り合いになるものを並べた行です。赤い縦の破線がマイルストーン（電源投入・音出し可能など）。
バーにカーソルを乗せると、場所・担当・前提作業・備考が出ます。

### ビュー3　平面図タイムライン

**多目的ホールのどこに、どの班が、その時刻にいるか**を示す図です。

- **▶ 再生**：15分ずつ自動で時間が進みます。最後まで行くと頭に戻ります
- **◀ ▶ ボタン / ← → キー / スライダー**：手で時刻を動かします
- 色の付いた札がその時刻に作業している班。**作業がないエリアは灰色**のままです
- エリアの右上に「2班」のように何班いるかが出ます。**3班以上になると枠が赤くなります**
  （ぶつかりそうな場所を見つけるための表示です）
- エリアが狭くて札を出しきれないときは「+1件」と出ます。中身は右のリストで全部読めます
- 右側に、その時刻の作業一覧・使用中の共有リソース・直前と次のマイルストーンが出ます

### 6-2b　本番シフト（10/17・10/18）

本番当日の多目的ホールステージの担当表です。**班ごとに区切って、縦に部員を並べています。**

- 上の **10/17（土）／10/18（日）** で日を切り替えます
- バーがその人の担当で、役割と時間が入っています。カーソルを乗せると備考が出ます
- **赤い破線と桃色の帯**が実行委の企画枠（本番）です
- **灰色**は多目的ホールが使えない時間（9:30前・18:00以降）です
- 担当を直すのは「編集」タブの**本番シフト**の表です

実行委のタイムスケジュール（2026年タイムスケジュール確定版）から読み取った企画枠は次のとおりです。

| 日 | 企画 | 時間 |
|---|---|---|
| 10/17(土) | 工大祭コンサート | 14:00〜15:30 |
| 10/18(日) | このレジスタにより | 14:00〜15:30 |
| 10/18(日) | サテライト | 16:00〜18:00 |

会場が使えるのは両日とも 9:30〜18:00 です。
**企画の時間が変わったら、「編集」タブで種別が「企画枠」の行を直してください。**

### 6-3　編集タブ

CSVを開かずに、画面の表で直せます。編集タブの中は
**「設営の作業」「本番シフト」「場所（会場）」の3つ**に分かれています。
直したいものを選んでください。どれにも**上にプレビュー**が付いていて、直すとその場で図になります。

**表の上にプレビューが出ます。** 直した結果がその場で図になるので、
別のタブに移動して確認する必要はありません。

（「設営の作業」のプレビュー）初期状態は **「両方」**。班別ガントと平面図が横に並びます（スマホでは上下）。
  時間の前後と「その時刻に誰がどこにいるか」を同時に確かめられます。
  片方だけ大きく見たいときは **班別ガント／平面図** に切り替えます
  （平面図はプレビューの中でも再生できます）
- **行を選ぶと、その作業がプレビューの中で濃く光ります**（他は薄くなります）。
  平面図を見ているときは、その作業が始まる時刻に自動で飛びます
- 表を下にスクロールしてもプレビューは画面の上に残ります
- 邪魔なときは **折りたたむ** で閉じられます（スマホでは追従せず、表の上に置かれます）

- **種別**：作業／マイルストーン／共有リソース。マイルストーンの行は終了時刻を持ちません（開始に揃い、入力欄が固定されます）
- **班・場所**：選ぶだけです。場所の選択肢は `layout.json` から作られます（名前は 6-5 で変えられます）
- **開始・終了**：時刻の入力欄です。スマホなら時計のダイヤルが出ます
- **担当**：複数なら `・` 区切り（例：`山田・佐藤`）
- **前提**：先に終わっている必要がある作業のIDを `,` 区切り（例：`S1,S2`）
- **↑ ↓ 複製 削除**：行の並べ替えと増減。複製すると新しいIDが自動で振られます。
  **削除は確認なしで消えますが、画面の下に出る「元に戻す」で戻せます**（15秒間）
- **IDを変えると、それを指している「前提」も自動で書き換わります**
- **＋ 作業を追加**：空の行を足します
- おかしいところがある行は**オレンジ色**になり、上に理由が出ます（時刻の前後が逆、存在しない場所、見つからない前提IDなど）
- スマホでは1行が1枚のカードになるので、横スクロールせずに直せます

### 6-3b　本番シフトのプレビュー

「本番シフト」を選ぶと、表の上にその日のシフト表が出ます。

- **10/17（土）／10/18（日）**のボタンで日を切り替えられます
- **表の行を選ぶと、その日に自動で切り替わって、その人のバーだけ濃くなります**
- 邪魔なときは「折りたたむ」で閉じられます

### 6-4　みんなの予定（全員で共有される自動保存）

案の欄の一番上が **「みんなの予定（自動保存）」** です。ここを選んで直すと、
**手を止めて1秒ほどで自動的に保存され、他の部員の画面にも反映されます。** 保存ボタンはありません。

- 他の人が直すと、開いたままの画面にも20秒以内に自動で入ってきます
- 右端に **「全員に保存しました 22:06（山田）」** のように、いつ誰が直したかが出ます
- **名札**の欄に自分の名前を入れておくと、それが相手に表示されます（任意）
- 自分が編集している最中に他の人も直した場合は、勝手に消さず
  **「相手の内容を読み込む／自分の内容で上書き」** を聞きます
- ネットが切れているときは「つながりません」と出て、その画面の中だけで編集できます。
  当日ネットが落ちても、図とPDFはそのまま使えます

**⚠ リンクを知っている人は誰でも書き換えられます。** ログイン不要にしている代わりの制約です。
消えて困る確定版は、必ず `data/schedule.csv` に反映して `sh tools/publish.sh` してください。
そちらが本物で、みんなの予定は「作業中の黒板」だと思ってください。

### 6-5　会場の配置を変える

編集タブの下に **「場所（会場の配置）」** があります。方眼の上に平面図が出ていて、
`layout.json` を開かずにそのまま作り替えられます。

- **場所をつかんで動かせます。** そのままドラッグするだけです
- **どの場所にも右下に小さな青い■が付いています。これをつまむと大きさが変わります**
  （先にクリックして選ぶ必要はありません）
- 5刻みに吸い付くので、並べるときに端が揃います。会場の枠からはみ出しません
- 右側の欄で、名前と位置・大きさを数字でも直せます（細かく合わせたいとき）
- **＋ 場所を追加**：平面図の上にある青いボタンです。押すと真ん中に新しい場所ができるので、
  右の欄で名前を付けて、好きな位置へドラッグしてください
- **この場所を削除**：選んでいる場所を消します
- **配布時の会場に戻す**：全部やり直せます
- 下の「名前をまとめて直す」で、7か所の名前を一気に打ち替えられます

直すとすぐ、ガント・平面図・表の「場所」欄すべてに反映されます。
みんなの予定を編集していれば、**配置の変更も他の部員に共有されます**。

**作業の割り当ては崩れません。** `schedule.csv` の「場所」は内部の id（`stage` や `foh`）で
結ばれていて、名前や位置はその見た目だけだからです。

**使っている場所を消すと警告が出ます。** 「A1：area "storage" が layout.json にありません」
のように、どの作業が行き場を失ったかが出て、その行がオレンジになります。
消した場所を戻すか、作業の「場所」を選び直してください。

配置を変えた状態で「CSVを書き出す」を押すと、**`layout.json` の書き出し欄も一緒に出ます**。
`data/layout.json` に貼れば、次のビルドからそれが既定の会場になります。

### 6-6　自分だけの案

編集内容は**自分だけの「案」としても保存できます。** こちらは自分の端末の中だけです。

- 「配布時の予定」を開いたまま何か直すと、**自動的に「案1」ができて**そこに保存されます
  （雨天案など、全員に見せる前に1人で試したいときに使います）
- 上の欄で **案を切り替え**られます。「配布時の予定」はいつでも元のまま残っています
- **名前を付けられます**（例：`雨天パターン`、`機材が間に合わない場合`）。
  右の欄に打って Enter か、他をクリックすれば変わります
- **＋ 別の案として保存**：いまの状態をコピーして別の案にします。
  1つの案を土台に枝分かれさせたいときに使います
- **この案を削除**：消した直後に「元に戻す」が出ます（15秒間）
- 右端に **自動保存 HH:MM** と出ます。ブラウザを閉じても次に開いたとき続きから出ます

**⚠ 案はその端末のブラウザにだけ残ります。** 自分のパソコンで作った案は、
他の部員の画面にも、自分のスマホにも出てきません。
案を開いている間は画面の上に青い帯が出てそのことを知らせます。
（プライベートウインドウなど保存できない画面では、その旨が帯に出ます）

全員の分に反映したいときは、下の手順でCSVを書き出して `schedule.csv` に貼り、`sh tools/publish.sh` を実行してください。

**みんなの分に反映するには**

1. 「編集」タブの **CSVを書き出す** を押す
2. 出てきた内容を **コピー**、または **ダウンロード**（共有リンクで開いた場合は保存の確認が出ます。
   保存ボタンが出ていない画面では「コピー」を使ってください）
3. `data/schedule.csv` にまるごと貼り付けて保存
4. `sh tools/publish.sh` を実行する（全員の画面に反映されます）

元の予定を見たいときは、案の欄で **「配布時の予定」** を選びます。
案は消えないので、あとで選び直せば続きから編集できます。

## 7. PDFで配る・貼り出す

右上の **「PDFで書き出す（A4横）」** を押すと印刷画面が出ます。
送信先（プリンタ）で **「PDFに保存」** を選べばPDFファイルになります。

紙に出すものは、ボタンの左の **「紙に出すもの」** で選べます。

| 選択 | 出るもの |
|---|---|
| 全部（ガント＋シフト） | 1枚目＝設営の班別ガント、2枚目＝10/17のシフト、3枚目＝10/18のシフト（計3枚） |
| 班別ガントだけ | 設営のガント1枚 |
| 本番シフトだけ | 10/17と10/18のシフト（計2枚） |

- **シフトは日ごとに1枚**になり、上に「本番シフト　10/17（土）　多目的ホールステージ」と日付が入ります。
  画面でどの日を見ていても、紙には**両日とも**出ます
- 「本番シフト」タブの **「この2日分をPDFで書き出す」** を押すと、シフトだけをすぐ出せます
- <kbd>⌘P</kbd> で直接印刷しても同じものが出ます
- 班の色もそのまま出ます
- **共有リンクで開いた場合、ボタンから印刷画面が開かないことがあります。**
  そのときは画面に案内が出るので、ブラウザのメニューから印刷（⌘P / Ctrl+P）を選んでください。
  配布版（`dist/index.html`）をダブルクリックで開いた場合はボタンがそのまま使えます
- 平面図タイムラインは時刻で変わるものなので紙には出ません
- どちらのタブを開いていても、出るのは必ず班別ガントです
- 右下に余白があるので、当日の書き込み欄に使えます

## 8. リンクで共有する（ブラウザで見てもらう）

平面図タイムラインは動かして見るものなので、紙ではなくリンクで配ります。

### 文字や図の大きさについて

図は**本来の大きさより大きくは引き伸ばしません**。ウィンドウを広げても文字が大きくなったりせず、
Chrome・Safari・Firefox のどれで開いても同じ見え方になります。
画面が狭いときは、図の枠の中で横にスクロールしてください。

それでも全体が大きい／小さいと感じるときは、ブラウザ自体の拡大率
（Mac は <kbd>⌘</kbd>+<kbd>0</kbd>、Windows は <kbd>Ctrl</kbd>+<kbd>0</kbd> で100%に戻ります）を確認してください。

### 画面が古いままのときは

ブラウザはページを最大10分そのまま使い回します。更新したのに変わらないときは、
**画面の上に緑の「新しい版が公開されました」**が出るので「読み込み直す」を押してください。
出てこない場合は、Mac は <kbd>⌘</kbd>+<kbd>Shift</kbd>+<kbd>R</kbd>、
Windows は <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>R</kbd> で読み込み直せます。

### 部員に配るリンク

**<https://greenapple822.github.io/kodaisai-gantt/>**

- **ログイン不要**。ChromeでもSafariでもスマホでも、リンクを踏むだけで開きます
- アプリのインストールも、アカウントも要りません
- 平面図は指で横にスクロールできます

### 予定を変えて、全員に反映する

`data/schedule.csv` を直したら、ターミナルでプロジェクトのフォルダに入って1行打つだけです。

```bash
sh tools/publish.sh
```

図の作り直し → 記録 → GitHubへ送信 まで全部やります。**1〜2分で公開ページが新しくなります。**
変更の内容をメモに残したいときは、後ろに付けてください。

```bash
sh tools/publish.sh "照明の吊り込みを30分延長"
```

図もPDFも平面図も元は同じ `schedule.csv` 1枚なので、直す場所は今までどおり1箇所だけです。

### 置き場所

- リポジトリ：<https://github.com/Greenapple822/kodaisai-gantt>
- 公開されているのは `docs/` フォルダの中身です。ここは `tools/build.py` が自動で作るので、手で触らないでください
- **公開リポジトリなので、書いた内容は誰でも見られます。** 個人の連絡先など、外に出したくないものは書かないでください

## 9. いまできること / まだできないこと

- できる：**ビュー1（班別ガント）**、**ビュー3（平面図タイムライン）**、
  **画面上での編集（ガント＋平面図の同時プレビュー付き）**、
  **全員で共有される自動保存**、**自分だけの案の自動保存**、
  **本番シフト表（班別・縦＝人）**、
  **平面図をドラッグしての会場編集（移動・大きさ・追加・削除・改名）**、**CSV書き出し**、
  共有リソースのレーン、マイルストーンの縦破線、3班以上の重なりの警告、データの間違い検出、**A4横のPDF書き出し**
- まだ：ビュー2（場所別ガント）、現在時刻の縦線、平面図を6コマ割りにした印刷、遅延入力、班フィルタ
